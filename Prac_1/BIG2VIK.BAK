#include<stdio.h>
#include<conio.h>
void main()
{
	clrscr();
	int a,b,c;
	printf("enter three numbers");
	scanf("%d%d%d",&a,&b,&c);
	if(a>b)
	{
		if(a>c)
			printf("greater is:%d",a);
		else
			printf("greater is:%d",c);
	}
	else
	{       if(b>c)
			printf("greater is:%d",b);
		else
			printf("greater is:%d",c);
	}
	getch();
}
